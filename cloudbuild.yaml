steps:
  # Step 1: Build Docker image
  - name: "gcr.io/cloud-builders/docker"
    args:
      [
        "build",
        "-t", "gcr.io/$PROJECT_ID/vibe-scraper:$SHORT_SHA",
        "-f", "Dockerfile",
        "."
      ]
  # Step 2: Push the Docker image
  - name: "gcr.io/cloud-builders/docker"
    args:
      [
        "push",
        "gcr.io/$PROJECT_ID/vibe-scraper:$SHORT_SHA"
      ]
  # Step 3: Deploy to Cloud Run
  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    entrypoint: gcloud
    args:
      [
        "run", "deploy", "vibe-scraper",
        "--image", "gcr.io/$PROJECT_ID/vibe-scraper:$SHORT_SHA",
        "--region", "us-central1",
        "--platform", "managed",
        "--allow-unauthenticated"
      ]

images:
  - "gcr.io/$PROJECT_ID/vibe-scraper:$SHORT_SHA"

options:
  logging: CLOUD_LOGGING_ONLY

